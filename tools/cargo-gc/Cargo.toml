[package]
name        = "cargo-gc"
description = "cargo gc — build, convert, and launch GameCube homebrew"
version.workspace    = true
authors.workspace    = true
edition.workspace    = true
license.workspace    = true

[[bin]]
name = "cargo-gc"
path = "src/main.rs"

# No external dependencies — pure std
