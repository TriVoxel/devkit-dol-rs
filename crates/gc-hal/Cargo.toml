[package]
name        = "gc-hal"
description = "GameCube/Wii hardware abstraction layer: VI, GX, SI, EXI, DSP, DVD, AI"
version.workspace    = true
authors.workspace    = true
edition.workspace    = true
license.workspace    = true

[dependencies]
gc-rt = { path = "../gc-rt" }

[features]
default = []
wii = ["gc-rt/wii"]

[features]
default = []
wii = ["gc-rt/wii"]
